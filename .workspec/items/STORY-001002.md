---
id: STORY-001002
type: STORY
title: "AI usage: record-usage and usage-report command-line tools"
status: Backlog
priority: high
assignee:
reporter: rdagum
created: 2026-09-09
updated: 2026-09-09
estimate:
labels:
  - feature
  - ai-usage
  - tooling
  - git
parent:
depends_on:
  - STORY-001001
blocks:
  - STORY-001004
related:
  - STORY-000011
context:
  - architecture
affected_paths:
  - tools/record-usage.js
  - tools/usage-report.js
  - test/tools.test.js
  - test/fixtures/transcripts/
  - README.md
  - CLAUDE.md
related_files:
  - docs/DESIGN-2026-09-ai-usage.md
acceptance_criteria:
  - node tools/record-usage.js ID --handle= --model= --input= --output= --cache-read= --cache-write= with optional --cache-write-1h=, --cost=, --purpose=, --ref=, --session=, --date=, --source=, --estimated, --dry-run and --force appends one run using the --key=value syntax of the other tools
  - The append goes through appendRunText from core/usage.js so every other line of the file is byte-identical and line endings are kept; when the front matter cannot be located the tool falls back to a full re-serialize and says so on stderr
  - updated is not changed by recording a run; an unknown ID is refused with exit code 1
  - --from-transcript=PATH accepts a Claude Code session .jsonl file or a session id under ~/.claude/projects, counts usage once per distinct message.id, skips rows whose model is <synthetic>, includes the session's subagents/*.jsonl files unless --no-subagents, sums input, output, cache read, cache write (5m) and cache write (1h) tokens per model, and appends one run per model with source claude-code, session set to the session id and estimated set when --since, --until or --branch trimmed the rows
  - --branch=NAME restricts transcript rows to that gitBranch and --item-from-branch derives the item ID from a workspec/<id>-… branch name in the rows
  - Recording a run whose source, session and model already exist on the item exits 1 unless --force
  - node tools/usage-report.js [PATH] [--by=status|assignee|model|type|label|epic] [--since=DATE] [--json] prints totals for the repository, and with an ID as the first argument prints that item's spent, budget and remaining; --json is stable enough for a shell script to read remaining; --strict exits 1 when the item is over budget
  - Exit codes follow validate-workspec.js: 0 ok, 1 refused or over budget under --strict, 2 could not load
definition_of_done:
  - test/tools.test.js runs both tools as child processes against throwaway repositories: a manual append on a file with CRLF and comments that is byte-identical elsewhere, creation of agent and runs on an item without them, duplicate refusal, a synthetic transcript fixture with repeated rows per message.id and a subagents directory, and the shape of the report's --json output
  - README "Tracking AI usage" shows the real commands and one sample report; the CLAUDE.md Commands block lists both tools
  - node --test and node tools/validate-workspec.js .workspec are clean
spec_version: 1.0
---

# Summary

Two zero-dependency Node tools over the model from STORY-001001: `record-usage.js` appends a run to an item — by hand, or imported from a Claude Code session transcript including its subagents — and `usage-report.js` prints roll-ups and one item's budget state. Together they are what an agent runs before and after a session.

# Business Context

An agent cannot read its own usage while it runs; the numbers exist only in the transcript on disk and are pruned after a retention period. The import tool is what turns a transient transcript into a durable fact on the item, and the report is the pre-run budget gate the daily routine needs.

# Technical Context

Reuse `tools/lib.js` (`loadWorkspec`, `parseArgs`, `NodeWorkspecFS`) and `test/load.js` like the existing tools; `parseArgs` accepts only `--flag` and `--key=value`, so the syntax is fixed to that. Transcript facts verified on 2026-09-09 (`docs/DESIGN-2026-09-ai-usage.md` 4.2): each API response is logged as several rows that repeat the same `message.id` and usage block, so naive summing over-reports several-fold; subagent transcripts live in `<session-id>/subagents/agent-*.jsonl` next to the parent file, with their own model; rows carry `gitBranch` and `cwd`; `<synthetic>` rows carry no usage. Routine runs with the Worktree toggle write to a different project directory (`…--claude-worktrees-<name>`), so session-id lookup must search every project directory whose name starts with the repository's.

# Requirements

- `tools/record-usage.js` with the manual flags and the transcript adapter as specified.
- `tools/usage-report.js` with repository and single-item modes, `--json` and `--strict`.
- A transcript fixture under `test/fixtures/transcripts/` (synthetic, small) with duplicated rows and a `subagents/` directory.
- README and CLAUDE.md command documentation.

# Implementation Notes

Print the appended run in `--dry-run` exactly as it would be inserted. Write the costs the report derives with at most four decimals. Keep all arithmetic in `core/usage.js`; the tools only parse arguments, read files and print.

# Risks

Transcript format is Claude Code's, not WorkSpec's, and may change; keep the adapter in one function with the field names in one place, and make a row without `message.usage` a skip, not a crash.

# References

- docs/DESIGN-2026-09-ai-usage.md, sections 4.1 to 4.3
- SKILL.md "AI Usage and Budgets"
- tools/renumber.js and tools/validate-workspec.js as the CLI pattern
