# Local, user-specific WorkSpec config (SPEC.md §14)
.workspec/config/user.local.yaml
.workspec-demo/config/user.local.yaml

# Claude Code worktrees (one per routine run); never committed.
.claude/worktrees/
